<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="font-sans">
    <!-- ✅ Fixed Navbar: stays on top with high z-index -->
    <nav class="w-full fixed top-0 left-0 flex justify-between items-center shadow-md z-50 bg-white px-8 py-4">
        <h3 class="text-3xl font-semibold text-blue-500">Scatch</h3>
        <% var loggedIn = typeof loggedIn !== "undefined" ? loggedIn : true %>
        <% if (loggedIn) { %>
            <div class="flex gap-5 text-lg font-medium">
                <a href="/shop">Shop</a>
                <a href="/cart">Cart</a>
                <a href="#">My Account</a>
                <a class="text-red-600" href="/users/logout/">Logout</a>
            </div>
        <% } %>
    </nav>